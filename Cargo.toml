[package]
name = "git-compression-stats"
version = "0.1.0"
edition = "2021"

[dependencies]
gix = { version = "0.72", default-features = false, features = ["blocking-network-client"] }
clap = { version = "4.5", features = ["derive"] }
human-size = "0.4"
indicatif = "0.17"
rayon = "1.10"
